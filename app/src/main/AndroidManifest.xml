<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <!-- ==================== NORMAL PERMISSIONS (Auto-granted) ==================== -->
  <!-- Core connectivity and system access -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <!-- Audio settings -->
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

  <!-- Foreground services for assistant functionality -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission
    android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"
    android:minSdkVersion="34" />
  <uses-permission
    android:name="android.permission.FOREGROUND_SERVICE_CAMERA"
    android:minSdkVersion="34" />

  <!-- ==================== DANGEROUS PERMISSIONS (Runtime request) ==================== -->
  <!-- Audio and voice -->
  <uses-permission android:name="android.permission.RECORD_AUDIO" />

  <!-- Camera -->
  <uses-permission android:name="android.permission.CAMERA" />

  <!-- Notifications (Android 13+) -->
  <uses-permission
    android:name="android.permission.POST_NOTIFICATIONS"
    android:minSdkVersion="33" />

  <!-- SMS and messaging -->
  <uses-permission android:name="android.permission.READ_SMS" />
  <uses-permission android:name="android.permission.SEND_SMS" />
  <uses-permission android:name="android.permission.RECEIVE_SMS" />

  <!-- Contacts -->
  <uses-permission android:name="android.permission.READ_CONTACTS" />
  <uses-permission android:name="android.permission.WRITE_CONTACTS" />

  <!-- Phone state -->
  <uses-permission android:name="android.permission.READ_PHONE_STATE" />

  <!-- ==================== SPECIAL PERMISSIONS (Settings redirect) ==================== -->
  <!-- System overlay for assistant UI -->
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

  <!-- System settings modification -->
  <uses-permission android:name="android.permission.WRITE_SETTINGS" />

  <!-- Package visibility (Android 11+) -->
  <uses-permission
    android:name="android.permission.QUERY_ALL_PACKAGES"
    android:minSdkVersion="30"
    tools:ignore="QueryAllPackagesPermission" />

  <!-- Foreground service for TickTick notifications (Android 14+) -->
  <uses-permission
    android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
    android:minSdkVersion="34" />

  <!-- ==================== HARDWARE FEATURES (optional for ChromeOS) ==================== -->
  <uses-feature
    android:name="android.hardware.camera"
    android:required="false" />
  <uses-feature
    android:name="android.hardware.telephony"
    android:required="false" />

  <!-- ==================== THIRD-PARTY PERMISSIONS ==================== -->
  <!-- Termux command execution -->
  <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

  <!-- TickTick task access -->
  <uses-permission android:name="com.ticktick.task.permission.READ_TASKS" />

  <application
    android:name=".Jarvis"
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.Jarvis">
    <activity
      android:name=".MainActivity"
      android:exported="true"
      android:label="@string/app_name"
      android:theme="@style/Theme.Jarvis">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- ==================== SERVICES FOR SPECIAL PERMISSIONS ==================== -->
    <!-- Notification Listener Service for notification access -->
    <service
      android:name=".service.NotificationListenerService"
      android:exported="false"
      android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
      <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
      </intent-filter>
    </service>

    <!-- Accessibility Service for system-wide assistance -->
    <service
      android:name=".service.AccessibilityService"
      android:exported="false"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
    </service>

    <!-- Assistant Foreground Service -->
    <service
      android:name=".service.AssistantService"
      android:exported="false"
      android:foregroundServiceType="microphone|camera" />

    <!-- ==================== AI BACKEND SERVICES ==================== -->
    <!-- Termux result receiver service -->
    <service
      android:name=".data.termux.TermuxResultReceiver"
      android:exported="false" />

    <!-- ==================== TICKTICK INTEGRATION ==================== -->
    <!-- TickTick foreground service for persistent task notification -->
    <service
      android:name=".service.TickTickService"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Persistent notification displaying user's TickTick tasks" />
    </service>
  </application>

  <queries>
    <package android:name="com.ticktick.task" />
  </queries>

</manifest>
